name: My First Workflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: welcome message
        run: echo "my first message"

      - name: list files
        run: ls -l

      - name: read files
        run: cat README.md
              